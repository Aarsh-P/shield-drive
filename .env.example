# PostgreSQL connection string (transaction pooler — used at runtime)
DATABASE_URL="postgresql://user:password@host:6543/dbname?pgbouncer=true"

# PostgreSQL direct connection string (session mode — used for migrations)
DIRECT_URL="postgresql://user:password@host:5432/dbname"
